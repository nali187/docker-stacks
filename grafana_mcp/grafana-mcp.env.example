# NICHT die echte .env committen - nur diese .example-Datei gehoert ins Repo.
# In Portainer als Stack-Umgebungsvariable setzen.

# Derselbe Service-Account-Token wie zuvor fuer claude-code geplant -
# jetzt lebt er hier, da der MCP-Server selbst die Grafana-Verbindung haelt.
GRAFANA_SERVICE_ACCOUNT_TOKEN=
