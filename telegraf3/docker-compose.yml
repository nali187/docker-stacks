version: "3.8"

services:
  telegraf:
    image: telegraf:1.36.3
    container_name: telegraf3
    volumes:
      - /home/nali/docker/telegraf3/telegraf.conf:/etc/telegraf/telegraf.conf:ro
    restart: unless-stopped